// para ter um código mais limpo declarar os tipos de ações em variáveis que serão utilizadas em vários lugares(actions, actions creators, reducers)
export enum ActionType {
  DEPOSIT = "deposit",
  WITHDRAW = "withdraw",
  BANKRUPT = "bankrupt",
}